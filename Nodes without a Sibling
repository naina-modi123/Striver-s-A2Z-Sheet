/*  A Binary Tree nodea
class Node
{
    int data;
    Node left, right;

    Node(int item)
    {
        data = item;
        left = right = null;
    }
}
*/
class Tree {
    private void helper(Node node, ArrayList<Integer> ans){
        if(node == null) return;
        
        if(node.left != null && node.right == null){
            ans.add(node.left.data);
        }
        
        if(node.left == null && node.right != null){
            ans.add(node.right.data);
        }
        
        helper(node.left, ans);
        helper(node.right, ans);
        
    }
    ArrayList<Integer> noSibling(Node node) {
        // code here
        ArrayList<Integer> ans = new ArrayList<>();
        helper(node, ans);
        if(ans.size() == 0){
            ans.add(-1);
        }
        Collections.sort(ans);
        return ans;
    }
}
